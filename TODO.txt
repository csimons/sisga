- Implement bit-string decoders.
  - Bit-string -> one (+/-) real. (done)
  - Bit-string -> two (+/-) reals, user-specified demarcation point.

- Allow user to specify fitness function at run-time.

- Implement additional fitness functions.

- Print periodic statistics during runs if verbose flag has been specified.
